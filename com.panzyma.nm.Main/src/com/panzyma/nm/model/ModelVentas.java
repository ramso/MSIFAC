package com.panzyma.nm.model;

public class ModelVentas
{
   static String TAG=ModelVentas.class.getSimpleName();
   public ModelVentas() {}
 
 /*  public static String getNumeroPedido(int numero) {        
       int cr = Integer.parseInt(Params.getValue("CerosRellenoNumRefPedido"));
       
       char[] num = new char[cr];
       for(int i=0; i < cr; i++) num[i] = '0';
       
       String strNum = new String(num);
       strNum = strNum + numero;
       return strNum.substring(strNum.length() - cr, strNum.length());
   }*/
   
}
